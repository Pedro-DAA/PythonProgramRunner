# PythonScriptRunner
Runs python programs can be edited to run different types

Program is set to default look into programs folder
If you want to make it look into a different folder make the var path = 'yourfolderpath'
Currently only set up to do to run cpp and python programs but can easily be made to run different ones
